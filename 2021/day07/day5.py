with open("/home/david/.advent_of_code/input/2021/07/input") as fd:
    data = [int(v) for v in fd.read().strip().split(",")]

print(data)


min_pos = min(data)
max_pos = max(data)


def p1_fuel(x, y):
    return abs(x, y)

def p2_fuel(x, y):
    

for target in range(min_pos, max_pos+1):
    for
    distance = abs(target)


part1 = min(*[v for v in data])